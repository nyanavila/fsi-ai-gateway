[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short --cov=app --cov-report=term-missing --cov-fail-under=70
markers =
    smoke: end-to-end smoke tests (require GATEWAY_URL env var)
